# Stone Shop — variables Docker (PC-9)
# Copier en `.env` à la racine avant `docker compose up`.
# Le fichier `.env` est ignoré par git (cf. .gitignore).

POSTGRES_DB=stone_shop
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
